[project]
name = "payments-data-lake-foundations"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
generate-payments = "src.ingestion.generate_data:main"
run-transform = "src.transform.bronze_to_silver:main"